# Git Commit Skill — 泛化颗粒度提交方法论

## 1. 核心原则

### 1.1 颗粒度铁律
每个 commit 只做一件事。一个 commit = 一个可独立 review 的变更单元。
- 可以独立 revert 而不破坏其他功能
- 可以独立 cherry-pick 到其他分支
- commit message 的 subject 一行就能说清楚

### 1.2 提交顺序 = 依赖拓扑排序
先提交被依赖的，后提交依赖方。编译链不能断。
```
新文件（无依赖） → 接口定义 → 实现代码 → 调用方 → 配置/文档
```

### 1.3 不跨模块、不跨层级
- 不同模块的改动分开提交（TurretEKF ≠ Tracker）
- 代码和文档分开提交（.cpp ≠ .md）
- 功能和配置分开提交（业务逻辑 ≠ xmake.lua）

---

## 2. 提交格式（Conventional Commits）

### 2.1 基本格式
```
type(scope): <subject — 英文，50字符以内，首字母小写，不以句号结尾>

<body — 详细说明，每行72字符以内>
- 变更点 1
- 变更点 2

Files:
- path/to/file.hpp: 变更说明
- path/to/file.cpp: 变更说明
```

### 2.2 type 选择决策树
```
新增功能模块/文件          → feat
修改已有功能逻辑           → feat（如果行为改变）/ refactor（如果行为不变）
重写模块（行为不变）        → refactor
纯注释/格式/命名清理        → refactor
修复 Bug                  → fix
构建配置/依赖/测试/文档     → chore
性能优化（行为不变）        → perf
```

### 2.3 scope 选择
- 用模块名：`Tracker`、`Pipeline`、`Detector`
- 用层级：`internal`（底层库）、`tests`、`docs`
- 跨模块用 `,` 分隔：`Pipeline,Predictor`

---

## 3. commit body 写法

### 3.1 写"为什么"，不写"改了什么"
- diff 已经告诉你改了什么，body 应该解释**设计决策**和**根本原因**
- 错误示范：`修改了 Tracker.cpp 的第 45 行，把 8 改成了 12`
- 正确示范：`kSkipToleranceThreshold 8→12：增加对偶发调度抖动的容忍度，不轻易触发 skipped 空转`

### 3.2 结构模板
```
<问题/背景一句话>

<解决方案概述>
- 关键技术点 1
- 关键技术点 2

<关键设计决策>
- 为什么选 A 不选 B（附简要理由）

Files:
- path/to/file: 一句话说明
```

---

## 4. 颗粒度拆分实战流程

### 4.1 第一步：`git status` 列出所有变更
```
modified:   Tracker.hpp
modified:   Tracker.cpp
modified:   Pipeline.hpp
modified:   Pipeline.cpp
new file:   TurretEKF.hpp
new file:   TurretEKF.cpp
modified:   xmake.lua
new file:   docs/changelog.txt
```

### 4.2 第二步：按依赖关系分组
```
组1（无依赖）:  TurretEKF.hpp, TurretEKF.cpp          ← 新模块
组2（依赖组1）: Tracker.hpp, Tracker.cpp               ← 调用 EKF
组3（依赖组2）: Pipeline.hpp, Pipeline.cpp             ← 调用 Tracker
组4（独立）:    xmake.lua, docs/changelog.txt          ← 配置/文档
```

### 4.3 第三步：逐组提交
```bash
git add 组1文件
git commit -m "feat(internal): add TurretEKF 9D Kalman filter..."

git add 组2文件
git commit -m "feat(Tracker): rewrite to dual-loop TurretEKF architecture..."

git add 组3文件
git commit -m "feat(Pipeline): add independent render thread..."

git add 组4文件
git commit -m "chore: update build config and changelogs"
```

### 4.4 合并条件
以下情况可以合并为一个 commit：
- 所有文件属于同一个逻辑模块
- 拆开会导致编译失败
- 总变更量 < 200 行且改动高度相关

---

## 5. 变更量控制

| 规模 | 行数 | 处理方式 |
|------|------|---------|
| 小 | < 100 行 | 可合并到相关 commit |
| 中 | 100~500 行 | 标准单个 commit |
| 大 | 500~1000 行 | 考虑拆分，但可接受 |
| 巨型 | > 1000 行 | 必须拆分 |

若单个文件变更 > 500 行，优先考虑：先提交基础设施（类型定义、接口），再提交实现。

---

## 6. 回溯修正（rebase 工作流）

### 6.1 修正上一个 commit
```bash
git add <遗漏的文件>
git commit --amend --no-edit    # 不改 message
git commit --amend               # 改 message
```

### 6.2 修正历史 commit（交互式 rebase）
```bash
git rebase -i HEAD~N
# 将需要修改的 commit 标记为 'edit' 或 'reword'
# edit: 修改内容和 message
# reword: 仅修改 message
```

### 6.3 合并多个 commit
```bash
git rebase -i HEAD~N
# 将需要合并的 commit 标记为 'squash' 或 'fixup'
```

### 6.4 注意
- 已 push 的 commit 不要 rebase（除非是个人分支）
- rebase 后需要 `git push --force-with-lease`

---

## 7. 提交前检查清单

- [ ] `git status` 确认无遗漏文件
- [ ] 每个 commit 的 type 和 scope 正确
- [ ] subject 行 ≤ 50 字符
- [ ] body 解释了"为什么"而非"改了什么"
- [ ] 无跨模块混合（如 Tracker + Pipeline 在同一 commit）
- [ ] 无跨层级混合（如代码 + 文档在同一 commit）
- [ ] 编译可通过（至少逻辑上依赖链完整）
- [ ] 无敏感文件（.env、密钥、大型二进制）

---

## 8. 快速参考卡片

```
type      → feat / fix / refactor / chore / perf / docs / test
scope     → 模块名（Tracker, Pipeline, Detector, internal）
subject   → 英文，≤50字符，小写开头，无句号
body      → 为什么这样改，设计决策，技术理由
Files     → 列出文件路径 + 一句话说明

正确示例：
feat(internal): add TurretEKF 9D Kalman filter for turret center tracking

Track the outpost turret center (xc, yc, za, yaw, r) instead of individual
armor plates. Eliminates the ID-freezing problem where a single slot
monopolizes all detections.

Architecture: 9D state vector with CV model propagation, Joseph-form
covariance update, yaw angle wrapping in predict().

Files:
- include/rm_autoaim/internal/TurretEKF.hpp: class definition
- src/internal/TurretEKF.cpp: full implementation

错误示例：
update code                          ← 无 type/scope
feat: 改了 Tracker                   ← 中文 subject，无 scope
feat(Tracker): fix bug              ← type 和内容矛盾
```